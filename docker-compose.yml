version: '3.8'

services:
  api:
    build: ./finpro-api
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./finpro-api:/app
      - /app/node_modules
    restart: unless-stopped

  web:
    build: ./finpro-web
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    volumes:
      - ./finpro-web:/app
      - /app/node_modules
    depends_on:
      - api
    restart: unless-stopped
